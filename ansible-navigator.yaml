ansible-navigator:
  ansible:
    config:
      path: ./ansible.cfg 

  execution-environment:
    container-engine: docker
    enabled: true
    image: project4-env:2.0
    pull:
      policy: missing
#    container-options:
#      - "--volume"
#      - "/root/.aws:/runner/.aws:ro"
#    environment-variables:
#      set:
#        AWS_PROFILE:
#        AWS_CONFIG_FILE:
#        AWS_SHARED_CREDENTIALS_FILE:
#        AWS_ACCESS_KEY_ID:
#        AWS_SECRET_ACCESS_KEY:
#        AWS_SESSION_TOKEN:
  playbook-artifact:
    enable: false
